<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".activities.DetailedPostActivity">
    <item
        android:id="@+id/action_detail_search"
        android:orderInCategory="100"
        android:title="@string/action_details_search_text"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_detail_share"
        android:orderInCategory="50"
        android:title="@string/action_detail_share_text"
        android:icon="@android:drawable/ic_menu_share"
        app:showAsAction="always"/>

</menu>